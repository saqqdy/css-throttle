function cssThrottle() {
	// .throttle {
	// 	animation: throttle 2s step-end forwards;
	// }
	// .throttle:active {
	// 	animation: none;
	// }
	// @keyframes throttle {
	// 	from {
	// 		pointer-events: none;
	// 		opacity: 0.99;
	// 	}
	// 	to {
	// 		pointer-events: all;
	// 		opacity: 1;
	// 	}
	// }
	// .throttle{animation:throttle 2s step-end forwards}
	// .throttle:active{animation:none}
	// @keyframes throttle{from{pointer-events:none;opacity:.99}to{pointer-events:all;opacity:1}}
}

export { cssThrottle as default }
